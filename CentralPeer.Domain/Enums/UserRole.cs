namespace CentralPeer.Domain.Enums;

/// <summary>
/// Defines the access level and role of user within the system
/// </summary>
public enum UserRole
{
    Student = 0,
    Tutor = 1,
    Admin = 2
}